Username Checker for Discord.
 - Thats a simple username checker that looks for available 3-4 letter usernames and saves the found ones in .txt file.

How to run?
  - Run `pip install requests`
  - Run the main.py

Proxies
  - It supports HTTPS proxies with format `protocol://username:pass@ip:port`

